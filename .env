# Convex PRODUCTION deployment (public client URL — safe to commit; not a secret).
# This is what the GitHub Pages build ships against.
# Local `npx convex dev` uses .env.local (the dev deployment) which takes precedence locally.
VITE_CONVEX_URL="https://aromatic-kudu-936.convex.cloud"
