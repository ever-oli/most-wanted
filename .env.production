# Production build env (vite build / mode=production).
# Loaded at HIGHER priority than .env.local, so local `npm run build:root`
# (e.g. Surge deploys) ships against PROD Convex, not the dev deployment.
VITE_CONVEX_URL="https://aromatic-kudu-936.convex.cloud"
